name: Skill Review
on:
  pull_request:
    paths: ['**/SKILL.md']

jobs:
  review:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: tesslio/skill-review-and-optimize@75158fcaec2dab4ea19476babc8652187d2535fd # main
        with:
          optimize: true
          tessl-token: ${{ secrets.TESSL_API_TOKEN }}
