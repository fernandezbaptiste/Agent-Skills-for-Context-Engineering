name: Skill Review
on:
  pull_request:
    paths: ['**/SKILL.md']

jobs:
  review:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: tesslio/skill-review-and-optimize@5cd6d0451ecfb6cbc94d123e94c770352d213a9c # main
        with:
          optimize: true
          tessl-token: ${{ secrets.TESSL_API_TOKEN }}
