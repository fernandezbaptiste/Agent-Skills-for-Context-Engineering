name: Skill Review
on:
  pull_request:
    paths: ['**/SKILL.md']

jobs:
  review:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: tesslio/skill-review-and-optimize@db67abfc3cdf1a669d87db4edd90977640ad8adb # main
        with:
          optimize: true
          tessl-token: ${{ secrets.TESSL_API_TOKEN }}
