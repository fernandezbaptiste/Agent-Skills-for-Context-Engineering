name: Skill Review
on:
  pull_request:
    paths: ['**/SKILL.md']

jobs:
  review:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: tesslio/skill-review-and-optimize@dc871a434044313e4ab28b9aeab30aebdba78578 # main
        with:
          optimize: true
          tessl-token: ${{ secrets.TESSL_API_TOKEN }}
